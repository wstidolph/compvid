import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sse.inventory',
  appName: 'ruskinv',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
