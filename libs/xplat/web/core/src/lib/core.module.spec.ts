describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { CompvidCoreModule } from './core.module';

describe('CompvidCoreModule', () => {
  it('should work', () => {
    expect(new CompvidCoreModule(null)).toBeDefined();
  });
});
*/
